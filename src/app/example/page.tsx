import Explorer from '../Explorer';

/**
 * The worked example, at its own address. Deterministic, so this URL is worth bookmarking and
 * safe to leave: nothing here belongs to a real account.
 */
export default function ExamplePage() {
  return <Explorer slot="example" />;
}
